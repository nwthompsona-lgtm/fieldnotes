# FieldReport REVIEW + ADMIN — environment config.
# Copy to `.env` (or `.env.local`) and adjust. Baked in at build time.

# Base URL of the FieldReport server. Default targets the local dev server.
VITE_API_BASE=http://localhost:8787
