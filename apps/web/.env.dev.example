# FieldReport REVIEW + ADMIN — DEV / STAGING env template.
# Set these on the Vercel "web-dev" project (production branch = develop).
# VITE_* are baked at build time, so redeploy if the dev server URL changes.

# Base URL of the DEV FieldReport server (Render fieldreport-server-dev).
VITE_API_BASE=https://fieldreport-server-dev.onrender.com
