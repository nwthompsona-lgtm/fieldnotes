{
  "name": "@fieldreport/web",
  "version": "0.1.0",
  "private": true,
  "description": "FieldReport REVIEW + ADMIN — the online surface where a superintendent reviews/edits the AI-drafted report (the trust gate) and the operator inspects raw-vs-polished quality.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fieldreport/contracts": "*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
